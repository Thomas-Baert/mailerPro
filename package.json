{
    "name": "mailer-pro-monorepo",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "turbo": "^1.11.3",
        "prettier": "^3.2.5",
        "typescript": "^5.3.3"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}